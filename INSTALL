regfont
Temporarily register/unregister fonts under Microsoft® Windows® 2000 and above.
Copyright © 2010–2013  David Purton <dcpurton@marshwiggle.net>

regfont needs to be compiled under using the MinGW-w64 compiler.  It uses GNU
getopt for option processing, and a compiler dependent flag for wildcard
matching so will not compile using any other build environment.

To build, run

./configure
make
make install

If building from a clean checkout from GitHub first run ./autogen.sh
